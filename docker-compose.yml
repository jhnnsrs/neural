version: "3.7"

services:
  neuron:
    build: neuronbook
    command: jupyter lab --port=90 --ip=0.0.0.0 --allow-root --notebook-dir=/notebooks/
    environment:
      - HOME=home
    volumes:
      - ./notebooks:/notebooks
      - .jupyt
    ports:
      - "200:90"